# Rupee Expense Tracker

A C++ expense tracker that saves data to JSON, with an HTML dashboard to visualize spending by category.

## How to run
g++ expense_tracker.cpp -o tracker
./tracker

Open index.html with Live Server to see the dashboard.
